const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}})},{threshold:.08});document.querySelectorAll('.hero-copy,.portrait-area').forEach(e=>{e.classList.add('reveal');observer.observe(e)});

const lightbox=document.getElementById('lightbox');const lightboxImage=document.getElementById('lightbox-image');const counter=document.getElementById('lightbox-counter');let gallery=[];let galleryIndex=0;function showGalleryImage(){if(!gallery.length)return;lightboxImage.src=gallery[galleryIndex];counter.textContent=gallery.length>1?`${galleryIndex+1} / ${gallery.length}`:'PROJECT VIEW'}document.querySelectorAll('.project-card-image').forEach(card=>{card.addEventListener('click',()=>{gallery=JSON.parse(card.dataset.gallery);galleryIndex=0;showGalleryImage();lightbox.classList.add('open');lightbox.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'})});function closeLightbox(){lightbox.classList.remove('open');lightbox.setAttribute('aria-hidden','true');document.body.style.overflow=''}document.querySelector('.lightbox-close').addEventListener('click',closeLightbox);document.querySelector('.lightbox-prev').addEventListener('click',()=>{galleryIndex=(galleryIndex-1+gallery.length)%gallery.length;showGalleryImage()});document.querySelector('.lightbox-next').addEventListener('click',()=>{galleryIndex=(galleryIndex+1)%gallery.length;showGalleryImage()});lightbox.addEventListener('click',e=>{if(e.target===lightbox)closeLightbox()});document.addEventListener('keydown',e=>{if(!lightbox.classList.contains('open'))return;if(e.key==='Escape')closeLightbox();if(e.key==='ArrowLeft'&&gallery.length>1){galleryIndex=(galleryIndex-1+gallery.length)%gallery.length;showGalleryImage()}if(e.key==='ArrowRight'&&gallery.length>1){galleryIndex=(galleryIndex+1)%gallery.length;showGalleryImage()}});
